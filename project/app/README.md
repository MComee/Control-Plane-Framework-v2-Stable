# Application Workspace

This directory is where implementation code lives.

The framework does not dictate language, runtime, or stack.
Implementation choices are project-specific and must remain bounded by the active handoff under `project/now/`.
