# Core Vision

This file is protected doctrine.

The framework exists to preserve project truth while guiding AI-assisted development through controlled repository state.

Core requirements:
- one repository instance controls one project
- planning state lives in repository files
- active work is handed off through `project/now/`
- evidence is written under `project/evidence/`
- root `README.md` remains a fully recursive exact map of tracked repository structure
