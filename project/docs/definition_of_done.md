# Definition of Done

Work is done only when:
- the intended file changes are complete
- allowed and forbidden path boundaries were respected
- validation requirements passed
- required evidence was written under `project/evidence/`
- the root `README.md` tree exactly matches tracked repository structure
