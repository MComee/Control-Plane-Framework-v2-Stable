# Decisions

## D-001
Use one repository instance to control one project under `project/`.

## D-002
Treat the root `README.md` as a required recursive map of tracked repository structure.

## D-003
Use `project/now/` as the active-work handoff layer and `project/evidence/` as the only evidence surface.
