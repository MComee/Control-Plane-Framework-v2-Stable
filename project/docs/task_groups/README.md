# Task Groups

This directory defines task groups that organize related tasks under specific features.

Grouping logic:
- One task group belongs to one feature.
- A task group contains ordered atomic tasks.
- Group completion is based on explicit exit conditions.

Use `framework/templates/task-group-template.md` for structure consistency.
